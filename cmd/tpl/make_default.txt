{{template "base_pre" .}}
modules := $(shell mbt provide modules)
modules := $(subst ],,$(subst [,,$(modules)))

modules:
{{"\t"}}@for a in $(modules) ; do \
            if [[ $$a = "ui5app" ]]; then\
            echo first: $$a ; \
            else \
            echo second: $$a ; \
            fi \
        done
