ID: testmta
_schema-version: '3.2'
version: 1.0.0

modules:
  - name: command_with_timeout
    path: command_with_timeout
    build-parameters:
      builder: custom
      commands:
        - sleep 1
      timeout: 2s