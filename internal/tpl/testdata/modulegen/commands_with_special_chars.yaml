ID: testmta
_schema-version: '3.2'
version: 1.0.0

modules:
  - name: commands_with_special_chars
    path: commands_with_special_chars
    build-parameters:
      builder: custom
      commands:
        - sh -c 'echo "a"'
        - echo "a\b"