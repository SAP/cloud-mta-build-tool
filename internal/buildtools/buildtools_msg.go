package main

const (
	readFailedMsg       = `could not generate configuration when reading the "%s" file`
	createFailedMsg     = `could not generate configuration when creating the "%s" file`
	populationFailedMsg = `configuration generation failed when populating the content`
)
