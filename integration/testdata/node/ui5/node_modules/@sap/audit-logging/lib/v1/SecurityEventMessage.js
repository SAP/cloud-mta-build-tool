'use strict';

var common = require('./common');
var SecurityEventBase = require('../base-messages/SecurityEventBase');

module.exports = SecurityEventMessage;

function SecurityEventMessage(content, transport) {
  SecurityEventBase.call(this, content, transport);
}

require('util').inherits(SecurityEventMessage, SecurityEventBase);

SecurityEventMessage.prototype.log = common.log;
