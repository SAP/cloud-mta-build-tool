'use strict';

var utils = require('../utils');

module.exports = {
  log: log
};

function log(callback) {
  utils.validate.callback(callback);
  this._validateContent();
  this._transport.log(this, callback);
}
